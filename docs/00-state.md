<div dir="rtl">

# מצב הפרויקט בהקפאה

**הוקפא 4.8.2026.** המפה חיה ועומדת בפני עצמה. המסמך הזה הוא נקודת הכניסה למי שחוזר לפרויקט, ובכלל זה אני עצמי בעוד חצי שנה.

**חי:** https://matanhakim.github.io/iicp-emek-yizrael-map/
**ריפו:** https://github.com/matanhakim/iicp-emek-yizrael-map (ציבורי)

## מה עומד

| | |
|---|---|
| נקודות במאגר | 1,038 |
| נקודות מתפרסמות | 928 |
| אומתו בתוך גבול השיפוט | 752 |
| ללא קואורדינטה, בטבלה בלבד | 45 |
| אושרו ביותר ממקור אחד | 668 |
| ודאות ממוצעת | 86% |
| נקודות בודאות 95% ומעלה | 400 |
| שלמות סטטוסים ממוצעת | 24% |
| קטגוריות | 726 ארכאולוגי, 203 היסטורי, 116 תרבות |

הבדיקה העצמית `python src/audit.py` עוברת **בלי אף מפגע חמור**: אפס הפרות סמכות בסטטוסים, אפס אי-התאמות בין שיוך לגבול לבין המרחק המחושב, אפס סתירות בין ITM ל-WGS84 באותה רשומה, אפס קטגוריות, טיפוסים, תקופות וערכי סטטוס לא מוכרים.

## הדבר החשוב ביותר שלא נעשה

**סבב האימות מרובה הסוכנים לא רץ.** הוא נכתב במלואו, הופעל, וכל עשרת הסוכנים נכשלו על מגבלת שימוש בסשן. מתן החליט ב-3.8.2026 לוותר עליו לצורך פגישה שהתחילה.

המשמעות המדויקת: **הודאות במפה מחושבת ואינה מאומתת מול מקורות חיצוניים נקודה-נקודה.** הבדיקות המבניות עוברות במלואן, אבל אף אחד לא אימת מול ויקיפדיה, רשות העתיקות או המועצה לשימור שאתר מסוים אכן קיים, אכן שם, ואכן מהתקופה שרשומה לו. יעד 95% שהוגדר לפרויקט מושג בחישוב ולא בהוכחה.

הסקריפט שמור ב-`workflows/02-verification.js` (עשרה סוכני אימות בעדשות שונות, ואחריהם סוכני הפרכה שמנסים לשבור כל טענה מערכתית). להרצה חוזרת:

```
Workflow({ scriptPath: 'workflows/02-verification.js' })
```

עשרת הסוכנים כותבים ל-`data/verify/<agent>.json` וההכרעות מוחלות מרכזית ולא בידי הסוכנים.

## שאריות מתועדות

| מה | כמה | איפה |
|---|---|---|
| זוגות שייתכן שהם אותו מקום ולא אוחדו | 30 | `data/out/audit.json` |
| נקודות בשם אנגלי בלבד, רוב מערות בית שערים | 22 | `audit.json`, סוג `name_latin_only` |
| צמדים שנחסמו במזהה מתנגש | 752 | `data/out/review_queue.json` |
| פריטים בתור בדיקה שעלו לפני השטח | 783 | אותו קובץ |
| רשומות חפירה שאינן מציינות אתר ולכן לא הפכו לנקודות | 622 | `docs/sources/iaa_discover.md` |

לא הומצא שם עברי לאף נקודה, ולא הושלמה אף קואורדינטה, תקופה או סטטוס שלא נמצאו במקור.

## עצמאות התיקייה

`data/raw/` הוא 336 מ"ב של הורדות מקוריות ו-`data/interim/` הוא 83 מ"ב של מטענים מנורמלים. שניהם מוחרגים מגיט. כדי שהריפו יוכל לבנות את עצמו בלעדיהם, `data/frozen/` מחזיק את הקלט שהצינור קורא בדחיסת gzip, **6.9 מ"ב, ונשמר בגיט**. `src/paths.py` מגשר על שני המצבים ואף קורא אינו צריך לדעת באיזה מהם הוא נמצא.

שכפול טרי של הריפו בונה את המפה כך:

```
python src/adapters.py && python src/harmonize.py && python src/build_site.py
```

לרענון הארכיון מעותק עבודה: `python src/freeze.py`. לבדיקת מה קיים: `python src/freeze.py --check`.

המקורות הגולמיים עצמם אינם בגיט, אבל `docs/sources/*.md` מתעד לכל אחד את נקודות הקצה המדויקות, פרמטרי הבקשה, הסכמה המלאה, המגבלות ותאריך השליפה, ולכן אפשר לשלוף אותם מחדש.

## מפת הקבצים

| | |
|---|---|
| `src/schema.py` | אוצר המילים היחיד: קטגוריות, טיפוסים, 18 תקופות, 12 צירי סטטוס, מרשם המקורות, כללי הקדימות |
| `src/hebrew.py` | נרמול שמות עבריים והתאמה ביניהם |
| `src/geo.py` | המרות ITM ו-ICS, ומחלקת `Jurisdiction` לבדיקת הכלה |
| `src/plus_code.py` | פענוח Plus Code של גוגל, אופליין |
| `src/vocabmap.py` | תרגום אוצר המילים העברי של המקורות לאוצר המילים המבוקר |
| `src/adapters.py` | מתאם לכל אחד מתשעת המקורות |
| `src/harmonize.py` | התאמה, אשכול, מיזוג, ניקוד |
| `src/audit.py` | בדיקה עצמית דטרמיניסטית |
| `src/build_site.py` | בניית המטען לאתר, כולל השחרת פרטי קשר פרטיים |
| `src/paths.py` | פתרון נתיבי קלט, עותק עבודה מול ארכיון קפוא |
| `src/freeze.py` | בניית הארכיון הקפוא |
| `src/test_core.py` | בדיקות ליבה, חייבות לעבור לפני שינוי ב-`src/` |
| `src/test_plus_code.py` | אימות המפענח מול 27 שורות שיש להן גם קוד וגם קואורדינטה |
| `site/` | האתר הסטטי. GitHub Pages מפרסם אותו דרך Actions |
| `data/manual/additions.json` | תוספות ידניות, נשמר בגיט |
| `docs/sources/` | מסמך לכל מקור |
| `docs/02-harmonization.md` | ההכרעות המלאות של ההרמוניזציה |
| `workflows/` | סקריפטי שני גלי הסוכנים |

## החלטות שלא כדאי לבטל בלי לקרוא קודם

1. **הגבול הוא רב-מצולע עם ששה חורים.** לא bounding box ולא convex hull. עפולה, נצרת, נוף הגליל, מגדל העמק, יקנעם עילית ורמת ישי אינן בתחום.
2. **קו 1700 הוא קדימות משפטית.** רישום של רשות העתיקות גובר על תאריך מאוחר יותר.
3. **שם גנרי אינו מאחד בלי הסכמת יישוב.** בעמק יש בית עם בכל מושב.
4. **מספר אתר של סקר אינו מזהה עתיקות.** אתר 1 קיים בכל גיליון סקר בארץ.
5. **עמוד ענן אינו נשלף אוטומטית.** ההבהרה המשפטית שלו אוסרת זאת. עובדות משם נכנסות רק כשאדם מסר אותן ידנית, והתיאורים נכתבים מחדש.
6. **חפירה היא אירוע ולא מקום.** רשומות חפירה מעשירות את האתר שהן שייכות לו ואינן הופכות לנקודות.
7. **פרטי קשר של רשומות פרטיות מושחרים בפרסום** ונשמרים במאגר הפנימי.

## אם ממשיכים

לפי סדר התועלת:

1. להריץ את סבב האימות. זה הפער היחיד שנוגע באמינות שמוצגת לציבור.
2. לטפל ב-30 זוגות הכפילות ובתור הבדיקה.
3. לאמת פעילות של מוסדות תרבות מול מקורות ראשוניים. `activity` ידוע ב-27 מתוך 116.
4. לשקול תת-דומיין של המכון, `emek-map.iicp.org.il`, דרך רשומת DNS ב-ISOC.
5. הצינור מנוסח כך שרשות אחרת דורשת גבול חדש ומתאמים חדשים בלבד. אוצר המילים, ההתאמה, הניקוד והאתר אינם תלויים בעמק יזרעאל.

</div>
