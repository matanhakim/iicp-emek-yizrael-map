<div dir="rtl">

# הוראות עבודה – iicp-emek-yizrael-map

מפה אינטראקטיבית ציבורית של אתרי תרבות ומורשת בתחום השיפוט של **מועצה אזורית עמק יזרעאל**, מטעם המכון הישראלי למדיניות תרבות. שלוש קטגוריות: אתר ארכאולוגי (עד 1700, רשות העתיקות), אתר היסטורי (אחרי 1700, המועצה לשימור אתרי מורשת), ומוסד תרבות ואומנות פעיל.

כללים חוצי-פרויקטים (שפה, em-dash, RTL, גיט, אמינות, אוטונומיה, פעולות חיצוניות) מוגדרים ב-`~/.claude/CLAUDE.md`. הקשר ארגוני: `../iicp-admin/context/`.

## שלושה עקרונות שקובעים הכל

1. **המאגר שומר טענות ולא עובדות.** לכל ערך שנפתר יש `provenance` עם מזהי המקורות שתומכים בו, וכל הטענות הגולמיות נשמרות ב-`claims` כולל אלה שהפסידו. אין לעדכן ערך במקום; מוסיפים טענה ומחשבים הכרעה מחדש.
2. **מקור אחד לאמת לכל שדה, כתוב מראש.** `schema.PRECEDENCE` קובע איזה מקור מנצח בכל שדה, כי הסמכות משתנה לפי השדה. שדות סטטוס משפטי מוגדרים ב-`schema.AUTHORITY_ONLY`: ערך שמגיע ממקור שאינו הסמכות נזרק ל-`unknown` ולא נכנס. סטטוס משפטי שגוי גרוע מפער מוצהר.
3. **פער מוצהר עדיף על ניחוש.** לכל ציר סטטוס יש `unknown` מפורש, והמפה מציגה אותו. אין להשלים קואורדינטה, תקופה או סטטוס שלא נמצא במקור.

## מבנה

- `src/schema.py` – אוצר המילים היחיד: קטגוריות, טיפוסים, תקופות, 12 צירי סטטוס, מרשם המקורות, כללי הקדימות.
- `src/hebrew.py` – נרמול שמות עבריים להצלבה בין מקורות. `distinctive()` מסלק ראשי שם גנריים (תל, בית, כפר, חורבת) כדי ששני שמות לא יתאימו רק בגלל הראש המשותף.
- `src/geo.py` – המרות ITM‏ (EPSG:2039) ו-ICS‏ (EPSG:28193) ל-WGS84 עם סירוב להמרה לא סבירה, ומחלקת `Jurisdiction` לבדיקת הכלה בגבול.
- `src/adapters/` – מתאם לכל מקור. הפלט: `data/interim/<source_id>.claims.json` לפי חוזה `CLAIM_RECORD`.
- `src/harmonize.py` – התאמה, אשכול, מיזוג, ניקוד ביטחון. הפלט: `data/out/sites.json`.
- `site/` – האתר הסטטי. `site/data/` נבנה מ-`data/out/`.
- `docs/sources/` – מסמך לכל מקור: נקודות קצה, סכמה מלאה, מגבלות, תאריך שליפה.

## כללי עבודה ייחודיים לריפו

1. **`data/raw/` ו-`data/interim/` מוחרגים מגיט.** כל תוצר חייב להיות ניתן לשחזור מלא מהקוד. `data/out/sites.json` כן נשמר, כי הוא התוצר.
2. **גבול השיפוט הוא רב-מצולע עם חורים.** המועצה עוטפת את עפולה, נצרת, מגדל העמק, יקנעם עילית, רמת ישי, קרית טבעון ואחרות, והן אינן בתחומה. אין להשתמש ב-bounding box או ב-convex hull; רק `Jurisdiction.contains` מול הגבול הרשמי.
3. **קו 1700 הוא קו חוקי, לא אסתטי.** חוק העתיקות התשל"ח-1978 מגדיר עתיקות כשרידים מלפני 1700. אתר עם תקופה אחת לפני 1700 הוא ארכאולוגי גם אם שימש אחר כך.
4. **בני אדם מחוץ לתחום.** שורות שמתייחסות לאמן או לאדם בודד מסומנות כמוחרגות, לא נמחקות.
5. **קידוד:** Python‏ `encoding='utf-8'`; PowerShell תמיד `-Encoding UTF8` ו-`-LiteralPath`. נתיב עברי שעובר מ-bash ל-Windows Python נשבר, ולכן קודם להעתיק לנתיב ASCII.
6. **`python src/test_core.py` חייב לעבור** לפני כל commit שנוגע ב-`src/`.

## פקודות

```
python src/test_core.py        # בדיקות ליבה
python src/harmonize.py        # בונה data/out/sites.json
python src/build_site.py       # מעתיק ומכווץ ל-site/data/
```

## כלים

PowerShell / Bash, git, gh CLI, Python 3.14 (pandas, geopandas, shapely, pyproj, rapidfuzz), Node לאימות פלטות צבע.

</div>
